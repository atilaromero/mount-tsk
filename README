# mount-tsk

Uses python sleuthkit bindings to mount the partitions of a forensic image.

## Usage

```
  mount-tsk.py img.dd 
```

It will create directories named as 'vol' + num, one for each partition, and mount the partition in read-only mode.
